import 'package:flutter/material.dart';

abstract class MAMSColors {
  /// Main Color
  static const mainColor = Colors.amber;

  /// Sub Color
  static const subColor = Color(0xffe2eaff);

  /// black
  static const black = Colors.black;
  static const mainBlack = Color(0xff333333);

  /// white
  static const white = Colors.white;

  /// grey
  static const grey = Colors.grey;
  static const background = Color(0xfff4f4f4);
  static const border = Color(0xffcccccc);
  static const placeholder = Color(0xffaaaaaa);
  static const button = Color(0xffdddddd);
  static const button2 = Color(0xffbbbbbb);
  static const description = Color(0xff777777);
}
